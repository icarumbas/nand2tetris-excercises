// if (in == 0) sets out = 1 else 0       

CHIP IsNegative {
    IN  
        in[16];  // 16-bit inputs  
    OUT 
        out; // 16-bit output

    PARTS:
    Mux(a=false, b=true, sel=in[15], out=out);
    
}